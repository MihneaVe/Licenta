# VCS
.git/
.gitignore
.gitattributes

# Python
__pycache__/
*.py[cod]
*.pyc
*.egg-info/
.venv/
venv/
env/

# Django runtime
*.log
db.sqlite3
db.sqlite3-journal
backend/db.sqlite3
backend/db.sqlite3-journal
urbanpulse_dev.sqlite3
urbanpulse_dev.sqlite3-journal
media/
staticfiles/
backend/staticfiles/

# Secrets / local config
.env
.env.local
.env.*.local

# Editor / OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# Frontend tooling (if any)
node_modules/

# Build artefacts
build/
dist/
*.egg

# Test / cache
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# React app - built into its own image (compose `frontend` service,
# context: ./frontend-react). The Django image needs none of it, so keep
# the whole subtree out of this (root) build context.
frontend-react/

