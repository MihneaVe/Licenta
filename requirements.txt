# ---------------------------------------------------------------------------
# CivicPulse backend — FastAPI + SQLAlchemy (replaced the former Django stack).
# ---------------------------------------------------------------------------

# Web framework + ASGI server
fastapi==0.136.3
uvicorn[standard]==0.49.0

# ORM + migrations + pgvector type
SQLAlchemy==2.0.50
alembic==1.18.4
pgvector==0.3.6
psycopg2-binary==2.9.6        # Postgres driver (Supabase)

# HTTP client (Ollama + Supabase REST)
requests==2.31.0

# RAG reranker — local cross-encoder BAAI/bge-reranker-v2-m3 (XLM-RoBERTa)
# loaded via raw transformers + torch. Embeddings + generation + grading +
# query-rewrite + HyDE all run on Ollama (qwen2.5:14b / nomic-embed-text) over HTTP.
transformers==4.37.0
torch==2.1.2
sentencepiece==0.1.99

# NLP batch tooling (interpreters/: sentiment, topic, NER) — used by the
# offline reprocessing scripts, not the web app.
spacy==3.7.2
langdetect==1.0.9

# Data collection (scrapers/: Reddit API + web scraping) — offline tooling.
praw==7.7.1
playwright==1.41.0
beautifulsoup4==4.12.2

# Geo (geo/: OpenStreetMap, geocoding, spatial)
osmnx==1.9.1
geopy==2.4.1
shapely==2.0.2

# Data processing
pandas==2.1.4
numpy==1.26.3

# Utilities
python-dotenv==1.0.0
joblib==1.3.2
